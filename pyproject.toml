[project]
name = "karapyner"
version = "0.0.1"
description = "Create a simple Karabiner-Elements configuration with Python"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0.2",
]
